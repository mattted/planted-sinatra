<!DOCTYPE html>
  <head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge, chrome=1" />

    <title>Planted</title>

    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <link rel="stylesheet" href="/stylesheets/main.css" />
  </head>
  <body>
    <div class="header">
      <h1>Planted</h1>
      <%# <p>some template text</p> %>
    </div>
    <div class="topnav">
      <a href="/home">Home</a>
      <a href="/plants">Plants</a>
      <a href="/plants/all/new-water-event">Water Events</a>
      <%= "<a href='/signup' style='float:right'>Sign Up</a>" if !logged_in? %>
      <%= "<a href='/login' style='float:right'>Login</a>" if !logged_in? %>
      <%= "<a href='/logout' style='float:right'>Logout</a>" if logged_in? %>
    </div>
    <div class="yield">
      <%= yield %>
    </div>
    <div class="footer">
      <p>This is a FOOOOOTER!!!!</p>
    </div>

  </body>
</html>
